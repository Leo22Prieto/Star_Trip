<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Are you sure ?</h5>
        <button type="button" class="btn-close modal-message" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= link_to "Of Course", trip_bookings_path(@trip.id, user_id: current_user.id, trip_id: @trip.id), method: :post, class: "btn btn-primary" %>
        <%# <p class="modal-message">Félicitations</p> %>
      </div>
    </div>
  </div>
</div>
